name: "Vta Client"
version: "1.0"

client:
  organization: Vta
  credentialStore:
    path: "/tmp/hfc-kvs/vta"
    cryptoStore:
      path: "/tmp/hfc-cvs/vta"
  connection:
    options:
      grpc.keepalive_time_ms: 120000